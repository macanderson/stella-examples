{
  "name": "stella-plugin-verify-ts",
  "version": "1.0.0",
  "private": true,
  "description": "The `verify` Stella plugin, in TypeScript. Zero runtime dependencies.",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "node --test test/wire.test.js",
    "pretest": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": ">=5.6.0"
  }
}
